# Custom Rules for Antigravity

- **Automatic GitHub Push**: Every time code writing or modifications are completed, push the current codebase state to the GitHub repository: `https://github.com/vincenzo-afk/NIGHTFALL.git` on the current branch (e.g., `main` or `master`).
