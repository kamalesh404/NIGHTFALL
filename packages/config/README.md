# packages/config

Game balance JSON/YAML files (classes, weapons, maps, xp, match, cosmetics, graphics, audio, bots).
See docs/10-configuration/ for the full spec. Every file here has a matching Zod schema in
packages/protocol/schemas/.
